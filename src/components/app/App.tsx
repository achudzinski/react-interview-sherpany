import * as React from 'react';

import {AppHeader} from "./AppHeader";
import "./App.scss";

export const App = () => {
    return (
        <div className="app">
            <AppHeader />
            abc
        </div>
    );
};
